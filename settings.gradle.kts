pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "voice-note-app"
include(":android-tts")
include(":android-speech")
